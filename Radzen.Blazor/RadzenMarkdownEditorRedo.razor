@using Radzen.Blazor.Rendering
@inherits RadzenMarkdownEditorButtonBase

<EditorButton Title="@Title" Click="@OnClick" Icon="redo" Disabled="@(Editor?.Disabled == true || Editor?.CanRedo != true)" />
