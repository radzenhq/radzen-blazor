@using Radzen.Blazor.Rendering
@inherits RadzenMarkdownEditorButtonBase

<EditorButton Title="@Title" Shortcut="@Shortcut" Click="@OnClick" Icon="checklist" Selected="@(Editor?.IsActive(MarkdownEditorCommands.TaskList) == true)" Disabled="@(Editor?.Disabled == true)" />
