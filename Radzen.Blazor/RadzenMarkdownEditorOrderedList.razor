@using Radzen.Blazor.Rendering
@inherits RadzenMarkdownEditorButtonBase

<EditorButton Title="@Title" Shortcut="@Shortcut" Click="@OnClick" Icon="format_list_numbered" Selected="@(Editor?.IsActive(MarkdownEditorCommands.OrderedList) == true)" Disabled="@(Editor?.Disabled == true)" />
