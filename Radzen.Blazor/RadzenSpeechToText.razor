@inherits RadzenComponent
@if (Visible)
{
    <RadzenButton @ref="_micButton" ButtonStyle="ButtonStyle.Light" Icon="mic" class="@GetCssClass()" Click="OnSpeechToTextClicked"
              title="Speech To Text" aria-label="Speech To Text"></RadzenButton>
}
