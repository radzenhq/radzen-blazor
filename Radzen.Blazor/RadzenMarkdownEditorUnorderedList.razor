@using Radzen.Blazor.Rendering
@inherits RadzenMarkdownEditorButtonBase

<EditorButton Title="@Title" Shortcut="@Shortcut" Click="@OnClick" Icon="format_list_bulleted" Selected="@(Editor?.IsActive(MarkdownEditorCommands.UnorderedList) == true)" Disabled="@(Editor?.Disabled == true)" />
