@using Radzen.Blazor.Rendering
@inherits RadzenMarkdownEditorButtonBase

<EditorButton Title="@Title" Shortcut="@Shortcut" Click="@OnClick" Icon="format_quote" Selected="@(Editor?.IsActive(MarkdownEditorCommands.Quote) == true)" Disabled="@(Editor?.Disabled == true)" />
