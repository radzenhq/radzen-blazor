@using Radzen.Blazor.Rendering
@inherits RadzenMarkdownEditorButtonBase

<EditorButton Title="@Title" Shortcut="@Shortcut" Click="@OnClick" Icon="code" Selected="@(Editor?.IsActive(MarkdownEditorCommands.Code) == true)" Disabled="@(Editor?.Disabled == true)" />
