@using Radzen
@using Radzen.Blazor.Spreadsheet
@using Radzen.Documents.Spreadsheet
<RadzenButton ButtonStyle="ButtonStyle.Base" Click=@OnInsertRowAfterClick Icon="add_row_below" ButtonType="ButtonType.Button" Disabled=@IsDisabled />
@code {
#nullable enable

    [Parameter]
    public Worksheet? Worksheet { get; set; }

    private bool IsDisabled => Worksheet == null || Worksheet.Selection.Cell == CellRef.Invalid;

    private void OnInsertRowAfterClick()
    {
        if (Worksheet == null)
        {
            return;
        }

        Worksheet.Commands.Execute(new InsertRowAfterCommand(Worksheet, Worksheet.Selection.Range.End.Row));
    }
}