@using Radzen.Blazor
@using Radzen.Blazor.Rendering
@inherits RadzenHtmlEditorButtonBase

<EditorButton Title=@Title Click=@OnClick Icon="redo" Disabled=@(!Editor.State.Redo) />

@code {
    protected override string CommandName => "redo"; 

    [Parameter]
    public string Title { get; set; } = "Redo";
}