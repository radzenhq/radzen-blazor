@using Radzen.Blazor.Rendering
@inherits RadzenMarkdownEditorButtonBase

<EditorButton Title="@Title" Shortcut="@Shortcut" Click="@OnClick" Icon="format_bold" Selected="@(Editor?.IsActive(MarkdownEditorCommands.Bold) == true)" Disabled="@(Editor?.Disabled == true)" />
