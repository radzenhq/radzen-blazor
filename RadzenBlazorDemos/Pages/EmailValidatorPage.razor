@page "/emailvalidator"
@page "/docs/guides/components/emailvalidator.html"

<RadzenText TextStyle="TextStyle.H2" TagName="TagName.H1" class="rz-pt-8">
    EmailValidator
</RadzenText>
<RadzenText TextStyle="TextStyle.Subtitle1" TagName="TagName.P" class="rz-pb-4">
    Demonstration and configuration of the Radzen Blazor <strong>EmailValidator</strong> component.
</RadzenText>

<RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.P" class="rz-pb-4">
    In this demo an <strong>Email</strong> input uses both <code>RadzenRequiredValidator</code> and <code>RadzenEmailValidator</code> to ensure the field is not empty and contains a valid email format, with a toggle for popup vs inline validation display and events logged to console.
</RadzenText>

<RadzenExample ComponentName="EmailValidator" Example="EmailValidatorConfig" Source="https://github.com/radzenhq/radzen-blazor/blob/master/Radzen.Blazor/RadzenEmailValidator.cs">
    <EmailValidatorConfig />
</RadzenExample>
