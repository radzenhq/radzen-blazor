@page "/checkbox"

<RadzenExample Name="CheckBox">
<div class="container-fluid">
    <div class="row px-3">
        <div class="col-md-4 p-3">
            <RadzenCard>
                <RadzenCheckBox @bind-Value=@checkBox1Value  Name="CheckBox1" TValue="bool" Change=@(args => OnChange(args, "CheckBox1 CheckBox")) />
                <RadzenLabel Text="CheckBox" Component="CheckBox1" Style="margin-left: 8px; vertical-align: middle;" />
            </RadzenCard>
        </div>
        <div class="col-md-4 p-3">
            <RadzenCard>
                <RadzenCheckBox @bind-Value=@checkBox2Value Name="CheckBox2" TriState="true" TValue="bool?" Change=@(args => OnChange(args, "TriState CheckBox")) />
                <RadzenLabel Text="TriState CheckBox" Component="CheckBox2" Style="margin-left: 8px; vertical-align: middle;" />
            </RadzenCard>
        </div>
        <div class="col-md-4 p-3">
            <RadzenCard>
                <RadzenCheckBox @bind-Value=@checkBox3Value Disabled="true" TriState="true" TValue="bool?" />
                <RadzenLabel Text="Disabled CheckBox" Component="CheckBox3" Style="margin-left: 8px; vertical-align: middle;" />
            </RadzenCard>
        </div>
    </div>
</div>
</RadzenExample>

<EventConsole @ref=@console class="mt-4" />

@code {
    bool checkBox1Value;
    bool? checkBox2Value;
    bool? checkBox3Value = true;

    EventConsole console;

    void OnChange(bool? value, string name)
    {
        console.Log($"{name} value changed to {value}");
    }
}