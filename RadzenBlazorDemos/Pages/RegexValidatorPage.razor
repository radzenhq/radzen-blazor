@page "/regexvalidator"
@page "/docs/guides/components/regexvalidator.html"

<RadzenText TextStyle="TextStyle.H2" TagName="TagName.H1" class="rz-pt-8">
    RegexValidator
</RadzenText>
<RadzenText TextStyle="TextStyle.Subtitle1" TagName="TagName.P" class="rz-pb-4">
    Demonstration and configuration of the Radzen Blazor <strong>RegexValidator</strong> component.
</RadzenText>

<RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.P" class="rz-pb-4">
    In this demo a <strong>ZIP code</strong> input uses <code>RadzenRequiredValidator</code> and <code>RadzenRegexValidator</code> with <code>Pattern="\d{5}"</code> to enforce a 5-digit format, with a toggle for popup vs inline validation display and events logged to console.
</RadzenText>

<RadzenExample ComponentName="RegexValidator" Example="RegexValidatorConfig" Source="https://github.com/radzenhq/radzen-blazor/blob/master/Radzen.Blazor/RadzenRegexValidator.cs">
    <RegexValidatorConfig />
</RadzenExample>
