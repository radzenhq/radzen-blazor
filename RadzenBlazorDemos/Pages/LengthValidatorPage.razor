@page "/lengthvalidator"
<RadzenText TextStyle="TextStyle.H2" TagName="TagName.H1" class="rz-pt-8">
    Blazor LengthValidator
</RadzenText>
<RadzenText TextStyle="TextStyle.Subtitle1" TagName="TagName.P" class="rz-pb-4">
    Enforce minimum and maximum text length on form inputs.
</RadzenText>

<RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.P" class="rz-pb-4">
    In this demo <strong>First Name</strong> and <strong>Last Name</strong> inputs each use two <code>RadzenLengthValidator</code> instances with <code>Min="3"</code> and <code>Max="10"</code> to enforce 3-10 character length constraints, with a toggle for popup vs inline validation display.
</RadzenText>

<RadzenExample ComponentName="LengthValidator" Example="LengthValidatorConfig" Source="https://github.com/radzenhq/radzen-blazor/blob/master/Radzen.Blazor/RadzenLengthValidator.cs">
    <LengthValidatorConfig />
</RadzenExample>
