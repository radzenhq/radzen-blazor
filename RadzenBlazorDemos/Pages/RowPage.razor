@page "/row"

<RadzenText TextStyle="TextStyle.H2" TagName="TagName.H1" class="rz-pt-8">
    Row
</RadzenText>
<RadzenText TextStyle="TextStyle.Subtitle1" TagName="TagName.P" class="rz-pb-4">
    The <code>RadzenRow</code> component is used to create a row in a responsive grid layout. It is primarily used in conjunction with <RadzenLink Text="RadzenColumn" Path="/column" /> component, but can also be used with other Radzen components, such as RadzenCard, to create responsive, visually appealing layouts.
</RadzenText>

<RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.P" class="rz-pb-4">
    In this demo four columns (<strong>Column 1-4</strong>) with varying heights (40px to 180px) demonstrate <code>RadzenRow</code> flexbox layout with dropdowns to adjust <code>AlignItems</code>, <code>JustifyContent</code>, and <code>Gap</code> properties.
</RadzenText>

<RadzenExample ComponentName="Row" Example="RowConfig">
    <RowConfig />
</RadzenExample>
