@page "/datagrid-virtualization-loaddata"

<RadzenText TextStyle="TextStyle.H2" TagName="TagName.H1" class="rz-pt-8">
    DataGrid <strong>Custom Virtualization</strong>
</RadzenText>
<RadzenText TextStyle="TextStyle.Subtitle1" TagName="TagName.P" class="rz-pb-4">
    RadzenDataGrid supports virtualization with custom data-binding scenarios. Handle the <code>LoadData</code> event as usual.
</RadzenText>

<RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.P" class="rz-pb-4">
    In this demo the OrderDetails grid includes a <strong>Refresh</strong> button to reload data, logs <code>Skip</code> and <code>Top</code> values to the console for debugging, and automatically resets to the first page when filters change.
</RadzenText>

<RadzenExample ComponentName="DataGrid" Example="DataGridVirtualizationLoadData">
    <DataGridVirtualizationLoadData />
</RadzenExample>