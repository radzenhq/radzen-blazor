@page "/dropdown-filtering"

<RadzenText TextStyle="TextStyle.H2" TagName="TagName.H1" class="rz-pt-8">
    Filtering case sensitivity and filter operator
</RadzenText>
<RadzenText TextStyle="TextStyle.Subtitle1" TagName="TagName.P" class="rz-pb-4">
    Configure filtering behavior with custom operators and case sensitivity.
</RadzenText>

<RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.P" class="rz-pb-4">
    In this demo the <strong>Customers</strong> dropdown uses <code>FilterOperator="StringFilterOperator.StartsWith"</code> with <code>bind-SearchText</code> pre-set to "al", displaying the current search text below the dropdown.
</RadzenText>

<RadzenExample ComponentName="DropDown" Example="DropDownFiltering">
    <DropDownFiltering />
</RadzenExample>

<RadzenText Anchor="dropdown-filtering#loaddata-event" TextStyle="TextStyle.H5" TagName="TagName.H2" class="rz-pt-8 rz-mb-6">
    Custom filtering with <strong>LoadData</strong> event
</RadzenText>
<RadzenExample ComponentName="DropDown" Example="DropDownFilteringLoadData">
    <DropDownFilteringLoadData />
</RadzenExample>