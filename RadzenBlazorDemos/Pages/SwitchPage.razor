@page "/switch"

<RadzenExample Name="Switch">
<div class="row p-4">
    <div class="col-md-6">
        <h4 class="mt-3">Switch</h4>
        <RadzenSwitch @bind-Value=@value Change=@(args => OnChange(args, "Switch")) />
    </div>
    <div class="col-md-6">
        <h4 class="mt-3">Disabled Switch</h4>
        <RadzenSwitch @bind-Value=@disabledValue Disabled="true" />
    </div>
</div>
</RadzenExample>

<EventConsole @ref=@console Class="mt-4" />

@code {
    bool value;
    bool disabledValue = true;

    EventConsole console;

    void OnChange(bool? value, string name)
    {
        console.Log($"{name} value changed to {value}");
    }
}