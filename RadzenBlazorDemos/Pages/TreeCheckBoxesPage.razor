@page "/tree-checkboxes"

<RadzenText TextStyle="TextStyle.H2" TagName="TagName.H1" class="rz-pt-8">
    Tree checkboxes
</RadzenText>
<RadzenText TextStyle="TextStyle.Subtitle1" TagName="TagName.P" class="rz-pb-4">
    Get or set the selected items of RadzenTree.
</RadzenText>

<RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.P" class="rz-pb-4">
    In this demo <strong>Categories</strong> and their <strong>Products</strong> display with <code>AllowCheckBoxes="true"</code>, products starting with "C" are pre-checked, the first category uses tri-state checkbox via <code>ItemRender</code> when no products are checked, and <code>bind-CheckedValues</code> logs selections to console.
</RadzenText>

<RadzenExample ComponentName="Tree" Example="TreeCheckBoxes">
    <TreeCheckBoxes />
</RadzenExample>
