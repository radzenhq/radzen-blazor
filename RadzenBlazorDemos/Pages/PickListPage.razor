@page "/picklist"

<RadzenText TextStyle="TextStyle.H2" TagName="TagName.H1" class="rz-pt-8">
    PickList
</RadzenText>
<RadzenText TextStyle="TextStyle.Subtitle1" TagName="TagName.P" class="rz-pb-4">
    Use Radzen Blazor <strong>PickList</strong> component to transfer items between two collections.
</RadzenText>

<RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.P" class="rz-pb-4">
    In this demo <strong>Customers</strong> can be moved between source and target lists with extensive controls for <code>Orientation</code>, button styling (Style, Variant, Shade, Size, Gap, JustifyContent), <code>AllowFiltering</code>, <code>Multiple</code> selection with <code>AllowSelectAll</code>, and <code>ItemRender</code> to disable items containing "a".
</RadzenText>

<RadzenExample ComponentName="PickList" Example="PickListConfig">
    <PickListConfig />
</RadzenExample>
